#include <stdio.h>

int main()
{
    int n, Max, Min, avg, product,sum;
    printf("Input integers, input 0 to end:\n");
    scanf("%d", &n);
    Max = n;
    Min = n;
    avg = 0;
    pruduct = 
    while (n != -1)
    {
        scanf("%d", &n);
        if (n > Max) Max = n;
        if (n < Min) Min = n;
    }
    printf ("Max: %d\n", Max);
    printf ("Min: %d\n", Min);
    return 0;

}